name: Entry Lineage

on:
  pull_request:
    paths:
      - 'index.html'
      - 'tools/reconcile_entry_lineage.py'
      - 'tests/test_reconcile_entry_lineage.py'
      - '.github/workflows/entry-lineage.yml'
      - 'docs/IDENTITY_AND_SCOPE.md'
      - 'docs/ENTRY_LINEAGE_BASELINE_2026-08-06.md'
      - 'docs/ENTRY_LINEAGE_BASELINE_2026-08-12.md'
      - 'docs/EDITORIAL_RENUMBERING_COMPLETION_2026-08-12.md'
  push:
    branches: [main]
    paths:
      - 'index.html'
      - 'tools/reconcile_entry_lineage.py'
      - 'tests/test_reconcile_entry_lineage.py'
      - '.github/workflows/entry-lineage.yml'
      - 'docs/IDENTITY_AND_SCOPE.md'
      - 'docs/ENTRY_LINEAGE_BASELINE_2026-08-06.md'
      - 'docs/ENTRY_LINEAGE_BASELINE_2026-08-12.md'
      - 'docs/EDITORIAL_RENUMBERING_COMPLETION_2026-08-12.md'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: entry-lineage-${{ github.ref }}
  cancel-in-progress: true

jobs:
  reconcile:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Syntax check
        run: python -m compileall -q tools/reconcile_entry_lineage.py tests/test_reconcile_entry_lineage.py

      - name: Unit tests
        run: python -m unittest discover -s tests -p 'test_reconcile_entry_lineage.py' -v

      - name: Generate and enforce source-grounded lineage evidence
        run: |
          python tools/reconcile_entry_lineage.py \
            --input index.html \
            --json entry-lineage.json \
            --markdown entry-lineage.md \
            --expect-pending 21 \
            --expect-drafted 70 \
            --expect-total 91 \
            --expect-pages 315 \
            --enforce

      - name: Verify evidence outputs
        run: test -s entry-lineage.json && test -s entry-lineage.md

      - name: Publish lineage summary
        if: always()
        run: |
          if [[ -f entry-lineage.md ]]; then
            cat entry-lineage.md >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload lineage evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: entry-lineage-${{ github.sha }}
          path: |
            entry-lineage.json
            entry-lineage.md
          if-no-files-found: warn
          retention-days: 30
